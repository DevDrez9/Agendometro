import 'dart:ui';

class AppThemeColors {
  AppThemeColors._();
  // 🔵 Color Primario: Usado para botones principales, encabezados y énfasis.
  static const Color secondary = Color(0xFFC563E8); // Azul vibrante

  // 🟢 Color Secundario: Para botones secundarios, enlaces y elementos destacados.
  static const Color primary = Color(0xFF8A32B3); // Verde éxito

  // ⚪ Fondo Claro: Usado como fondo principal en modo claro.
  static const Color backgroundLight = Color(0xFFE2C7F3); // Gris muy claro

  // ⚫ Fondo Oscuro: Para el modo oscuro o secciones destacadas.
  static const Color backgroundDark = Color(0xFF2B1A33); // Gris oscuro

  // ⚠️ Color de Alerta/Error: Para mensajes de error, alertas o validaciones.
  static const Color error = Color(0xFFE63946); // Rojo alerta

  // 🟡 Color de Advertencia: Para advertencias o estados pendientes.
  static const Color warning = Color(0xFFF4A261); // Amarillo

  // ✅ Color de Éxito: Para indicar acciones completadas con éxito.
  static const Color success = Color(0xFF34D399); // Verde éxito

  // 🔤 Color de Texto Principal: Alto contraste para legibilidad.
  static const Color textPrimary = Color(0xFF2E1A47); // Gris oscuro

  // 🔤 Color de Texto Secundario: Para textos menos relevantes.
  static const Color textSecondary = Color(0xFFA17DC5); // Gris medio

  static const Color textPlaceholder = Color(0xFFADB5BD);

  static const Color pago = Color(0xFF2DA84F);
}
